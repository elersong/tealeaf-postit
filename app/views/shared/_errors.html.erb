<% if object.errors.any? %>
  <div class="alert alert-error">
    <strong>The following errors prevented the form from being submitted:</strong>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>